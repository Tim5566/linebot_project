# CLAUDE.md — JellyStock 專案規則

> 本檔案是 JellyStock（jellystockdata.com）專案專屬規則，會與全域 CLAUDE.md（通用工作習慣）疊加生效。
> 定位：這是一個**已經在正式營運、有真實使用者、正在等 AdSense 審核**的網站，不是實驗性專案。
> 核心原則一句話：**除非我明確要求，否則不要動任何現有運作邏輯，尤其是排程、資料同步、路由對應這三塊。**

---

## 0.1 開始任何任務前

- **先讀 `ARCHITECTURE.md`**（專案根目錄），了解系統目前的資料流、檔案職責、排程設定、已知技術債，再開始動手。不要跳過這一步直接看程式碼猜架構。

## 0.2 架構文件同步更新規則（重要，長期有效）

**只要這次改動是「新增功能」或「刪除功能」，不論大小，完成後都必須同步更新 `ARCHITECTURE.md` 對應章節。** 包含但不限於：

- 新增或刪除任何網頁（連帶更新第 3、6 節的頁面清單）
- 新增或刪除任何 API 端點（連帶更新第 5.2 節的路由表）
- 新增或刪除任何排程任務（連帶更新第 5.4 節的 `SCHEDULE` 對照表）
- 新增或刪除任何 Firebase 資料路徑/欄位（連帶更新第 5.3 節的資料庫路徑結構）
- 新增或刪除任何環境變數、第三方套件、外部服務串接
- 新增或刪除任何核心檔案（例如把某支檔案拆成多支，或砍掉不用的檔案）

**執行方式：**
1. 功能改動完成後，主動列出「這次新增/刪除了什麼」。
2. 對照 `ARCHITECTURE.md` 內容，找出需要更新的章節，直接動手改，不要只在回覆裡口頭提醒我「建議更新文件」卻沒有真的去改。
3. 若改動小到不確定算不算「功能」層級（例如純文案調整、CSS 微調、修 bug 但沒改變功能行為），可以先問我要不要更新文件，而不是自己判斷後略過。
4. 更新完文件後，在回覆裡簡短列出「這次同步更新了 ARCHITECTURE.md 的哪些章節」，方便我確認。

**目的**：確保任何時候重新打開這個專案（包含新的 Claude Code session），`ARCHITECTURE.md` 都能準確反映系統當下的真實狀態，而不是停留在建立當下的舊快照。

---

## 0. 專案性質（先讀）

- 這是台股盤後資訊網站 + LINE Bot，服務對象是**一般散戶投資人**，不是工程師。
- 網站目前策略是「累積 Google 自然搜尋流量與信任度」以通過 AdSense 審核，任何改動都要考慮**會不會影響 SEO 或讓 AdSense 審核倒退**。
- 系統穩定性 > 新功能酷不酷。這是一個每天固定時間要準時抓資料、準時推播的系統，不是可以隨便重啟實驗的玩具專案。

---

## 1. 絕對不要主動改動的區域（除非我明確指名要改）

以下屬於「地雷區」，即使看起來可以優化、重構、簡化，**都不要主動動手**，只能提出建議讓我決定：

1. **`push_service.py` 的 `SCHEDULE` 排程設定**（時間點、label 對應、是否廣播）
   - 這是整個系統的時間中樞，改錯時間或順序會導致資料抓不到或推播錯誤時間點。
2. **`api_routes.py` 開頭的 `_throttled_get()` / `_get_stock_lock()`**（節流 + 鎖機制）
   - 這是防止 TWSE/TPEx 官方 API 把我們的 IP 封鎖的保護機制，動它有直接把網站打掛的風險。
3. **Firebase Realtime Database 的資料路徑結構**（`stock_data/{date}/...`、`top100_cache/{date}/...`、`stock_list/...`）
   - 前端、LINE Bot、排程三方都依賴這個路徑結構，改路徑等於同時要改三個地方，不能單獨改一處。
4. **`linebot_test.py` 的 self-ping 機制**
   - 目前是 Render 免費方案的必要保活手段，看起來像「多餘的背景執行緒」但不能刪，除非我告知已經升級付費方案。
5. **`.env`、`firebase_credentials.json`、`SYNC_SECRET`、`重要文件/` 資料夾**等機密資訊
   - `重要文件/` 是機密憑證備份資料夾，已在 `.gitignore` 中排除，但**不代表可以讀取或顯示其內容**——不要讀取、印出、摘要、或以任何形式在對話輸出、log、commit 中提及裡面的內容，除非我明確指名要處理裡面特定檔案。
   - 絕對不要印出內容、寫進 log、寫進任何回傳給前端的 JSON、或以任何形式出現在 commit / 對話輸出中。
6. **`api_routes.py` 裡「頁面路由 ↔ `stock_site/` 實體檔案」的手動一對一對應**
   - 這是手動維護，不是自動掃描資料夾。**新增任何網頁時，一定要同步：**
     - 在 `api_routes.py` 加對應路由
     - 更新 `sitemap.xml`
     - 確認 `robots.txt` 不會擋到新頁面
   - 少做任何一步都會變成「頁面存在但打不開」或「頁面存在但 Google 抓不到」。

> 如果任務描述聽起來會牽動以上任一項，**先停下來說明會影響到什麼，等我確認再做**，不要「順手一起改掉」。

---

## 2. 已知的技術債（可以提醒我，但不要自作主張動手修）

- `firebase_sync.py` 的 `_calc_top100()` 和 `post_Info.py` 的 `twse_top100()` / `otc_top100()` 疑似是重複邏輯的兩份實作。
  - 若任務是「修改買賣超前100的計算規則」，**兩個檔案都要一起改**，並且要主動提醒我「這兩處邏輯目前是分開的，要不要考慮之後合併」。
- `/api/visitor`、`/api/maintenance` 用途未完全確認，改動前先問我實際用法，不要用路由名稱猜測後就直接修改行為。

---

## 3. 對外使用者體驗（這是給一般散戶用的網站，不是工程師工具）

- 任何文案、按鈕文字、錯誤訊息，用**一般投資人看得懂的白話中文**，不要出現工程術語（例如不要顯示 `null`、`undefined`、raw error stack 給使用者看）。
- 資料查無結果、API 失敗、非交易日等情境，要有清楚、友善的提示文字，不能讓頁面空白或看起來像壞掉。
- 新增功能時的文案語氣要跟現有 20 篇教學文一致：專業、中性、教育性質，**不要用誇張行銷語氣**（例如「必賺」「穩賺不賠」「內線消息」這類用語一律禁止，這類字眼對金融資訊網站是高風險用詞）。
- 每個資料頁面本來就有「非官方網站、僅供參考、不構成投資建議」的免責聲明，**新增查詢工具或頁面時要延續加上**，不能省略。

---

## 4. Google AdSense / SEO 安全規則（審核尚未通過，特別敏感）

任何改動都要先問自己：**這會不會讓 AdSense 審核更難過、或讓已經過審後被停權？**

- **不要新增任何非 Google AdSense 的廣告聯播網或聯盟行銷連結**（歷史上已經清掉 funaicare、revolthem，不要因為新需求又加回類似的東西）。
- AdSense 程式碼目前用 HTML 註解關閉，**不要主動打開**，除非我明確說「審核過了，打開廣告」。
- 不要新增任何可能被 Google 視為「操縱排名」的手法：隱藏文字、關鍵字堆砌、購買外部連結、自動產生的低品質內容農場式頁面。
- 新增頁面／文章一定要同步更新 `sitemap.xml`，且內容要是**原創、對使用者有實質價值**的內容，不要為了衝頁面數量塞水分內容。
- `about.html`、`privacy.html`、`disclaimer.html` 這三個法律頁面內容改動要非常謹慎，這是 AdSense 審查必備頁面，改動前跟我確認要改的原因。
- `ads.txt` 檔案內容不要動，除非我明確要求（AdSense 帳號綁定資訊，改錯會讓廣告失效）。

---

## 5. 資料正確性（金融資料網站的底線）

- 顯示的法人買賣超、融資融券、注意股/處置股等資料，**一律以 Firebase 快取資料為準**，不要為了「補資料」讓前端或 LINE Bot 直接繞過 Firebase 去打 TWSE/TPEx API（這是架構設計上刻意避免的，見 ARCHITECTURE.md 第 4 節）。
- 若要新增資料欄位或計算邏輯，先確認 TWSE/TPEx 官方 API 的欄位定義，不要憑經驗猜測欄位意義（台股法人資料有「外資及陸資」「投信」「自營商（自行買賣）」「自營商（避險）」等容易搞混的分類）。
- 任何跟金額、股數、比例相關的數字計算，改動後要明確說明計算方式的變化，不要默默改變計算邏輯卻不說明（使用者可能會拿新舊數字對照，數字對不上會失去信任）。

---

## 6. 新增功能的標準流程

1. **先說明會動到哪些檔案**，尤其是否會碰到第 1 節的地雷區。
2. 若是新增查詢工具頁：
   - 建立 `stock_site/` 底下對應資料夾的 HTML
   - 在 `api_routes.py` 加頁面路由
   - 若需要新資料，在 `firebase_sync.py` 加同步邏輯 + 決定要不要進 `SCHEDULE`
   - 更新 `sitemap.xml`
   - 確認免責聲明語氣一致（見第 3 節）
3. 若是新增/修改 API 端點：
   - 確認是否需要節流保護（避免被官方 API 擋）
   - 確認回傳格式跟既有前端 fetch 邏輯相容
4. 完成後列出「這次改動摸到的檔案清單」，方便我快速 review diff。

---

## 7. 與全域 CLAUDE.md 的關係

全域規則（Think Before Coding / Simplicity First / Surgical Changes / Goal-Driven Execution）**照常適用**，本檔案是疊加，不是取代。衝突時以本檔案的專案特例為準（例如全域說「不要動不相關的舊 code」，本檔案第 1 節更進一步：某些區塊連「看起來相關」都不要動，要先問過我）。

---

## 8. 測試與部署流程（重要：`main` 一 push 就會自動上線）

Render 已與 GitHub 連動，**push 到 `main` 會自動觸發重新部署**。因此絕對不能「改完程式碼就直接 push main」，一定要走完以下流程，每個步驟都要等我明確確認才能進到下一步：

1. **完成程式碼修改後，先讓我在本機測試**（`http://127.0.0.1:5000/`），確認網站美編、版面、功能顯示都沒問題。
   - **在我明確說「測試沒問題／可以了」之前，不要 push 到 GitHub，也不要 merge 進 `main`。**
2. 我確認沒問題後，**push 前先建立日期命名的備份分支**（例如 `backup-2026-08-27`），內容是**目前 `main` 上線中的版本**（也就是還沒套用這次新改動之前的狀態），作為可回溯的還原點。
3. **只保留最新一份備份分支**：建立新備份後，把舊的備份分支（例如上一個 `backup-2026-08-25`）刪除，不要讓備份分支越堆越多。
4. 備份處理好後，才把這次修改 push 到 `main`，觸發 Render 自動部署。
5. 部署完成後，簡單複查一下正式網站（首頁、有改動的頁面、`/ping`）確認上線後一切正常。

**這個流程的目的**：確保「本機測試」「備份還原點」「正式上線」三件事有明確先後順序，任何一步都不能跳過或提前，尤其是「未經我確認就 push main」是絕對不行的。
